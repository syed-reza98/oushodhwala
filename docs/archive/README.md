# Archive / legacy artifacts

- `supabase/` (repo root) — Postgres migrations & config. **Reference only**; do not apply to MySQL.
- `_legacy/` — TanStack Start routes, Supabase-era ERP admin panels, stub clients, and unused `*.functions.ts`.

Live app code lives under `src/app`, `src/server`, and the remaining `src/components` / `src/lib` used by App Router.
